#if HAVE_CONFIG_H
#include <config.h>
#endif
#if HAVE_CONFIG_H
#include <config.h>
#endif
#include "marker.h"

void marker::localPropagate( Particle& ) {
 // Markers don't do anything.
}

void marker::localPropagate( JetParticle& ) {
 // Markers don't do anything.
}
